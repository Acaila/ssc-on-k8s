# Placeholder only.
apiVersion: v1
kind: Service
metadata:
  name: salt-master
  namespace: aria-config
spec:
  selector: { app: salt-master }
  # Keep this as an internal service stub for now. The eventual external
  # exposure model for minion traffic still needs to be validated in Kubernetes.
  type: ClusterIP
  ports:
    - name: tcp-publisher
      port: 4505
      targetPort: 4505
    - name: tcp-request
      port: 4506
      targetPort: 4506
