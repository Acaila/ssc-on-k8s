# Placeholder only.
# This namespace manifest reflects the intended target namespace for later
# Kubernetes work, but the repo's validated workflow today is image building
# and local Docker-based testing.
apiVersion: v1
kind: Namespace
metadata:
  name: aria-config
  labels:
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/enforce-version: latest
    pod-security.kubernetes.io/audit: baseline
    pod-security.kubernetes.io/warn: baseline
