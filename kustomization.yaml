# Placeholder only.
# The Kubernetes manifests in this repo are design-direction artifacts and are
# not yet maintained as a validated end-to-end deployment bundle.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: aria-config
resources:
  - namespace.yaml

# The remaining manifests are intentionally left out of the default kustomize
# bundle until the Kubernetes deployment path has been validated end to end.
# Keep them as reference material only for now.
