# Placeholder only.
apiVersion: v1
kind: Service
metadata:
  name: raas
  namespace: aria-config
spec:
  selector: { app: raas }
  ports:
    - name: api
      port: 443
      targetPort: 443
  type: ClusterIP
